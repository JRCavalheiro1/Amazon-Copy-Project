import { styled } from "styled-components";

export const Container = styled.div`
    .sugest-close-modal {
        position: fixed;
        top: 1%;
        left: 19.3%;
        z-index: 2;
        cursor: pointer;
    }

`;